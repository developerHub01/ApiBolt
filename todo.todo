## History feature
  - have to update history right after get response
  - have to save full request details like url, name, method, params, headers, body details, authorization and inherit-auth-id
  - and only meta info of response not even response body or cookie, headers
  
  - load metadata only when open history widget and when adding new history than just push the new meta-data of history in in-memory and each time updating in-memory of history trancat the list and keep only recent N number of meta-data
  - history details will laod on demand only when click on the history
  - after closing or changing selected history item clear history details imediatly to keep in-memory light
  
  - history cant be editable but you can open it in main/current then it will replace all data in main/current and send that request but cant edit history request.