export type TGlobalEnvType =
  | "$rand.uuid"
  | "$rand.shortId"
  | "$rand.int"
  | "$rand.float"
  | "$rand.hex"
  | "$rand.octal"
  | "$rand.binary"
  | "$rand.romanNumeral"
  | "$rand.bool"
  | "$rand.alphaNum"
  | "$rand.word"
  | "$rand.lorem"
  | "$rand.sentence"
  | "$rand.string"
  | "$rand.firstName"
  | "$rand.lastName"
  | "$rand.fullName"
  | "$rand.gender"
  | "$rand.email"
  | "$rand.phone"
  | "$rand.username"
  | "$rand.avatar"
  | "$rand.imageUrl"
  | "$rand.domain"
  | "$rand.domainSuffix"
  | "$rand.ip"
  | "$rand.ipv4"
  | "$rand.ipv6"
  | "$rand.jwt"
  | "$rand.protocol"
  | "$rand.port"
  | "$rand.password"
  | "$rand.city"
  | "$rand.buildingNumber"
  | "$rand.continent"
  | "$rand.country"
  | "$rand.direction"
  | "$rand.language"
  | "$rand.latitude"
  | "$rand.longitude"
  | "$rand.state"
  | "$rand.street"
  | "$rand.zipCode"
  | "$rand.timeZone"
  | "$rand.anytime"
  | "$rand.futureTime"
  | "$rand.pastTime"
  | "$rand.recentTime"
  | "$rand.month"
  | "$rand.weekday"
  | "$rand.color"
  | "$rand.colorHex";
